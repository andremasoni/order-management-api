# Copy to .env and fill in before running docker compose.
# Generate the JWT secret with: openssl rand -base64 32
JWT_SECRET=

# Optional: creates the first ADMIN user at startup when both are set.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
